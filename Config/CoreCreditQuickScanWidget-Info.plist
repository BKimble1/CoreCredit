<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!--
	  Partial Info.plist for the CoreCreditQuickScanWidget extension.

	  `NSExtension` has to be declared in a real file. It is a nested dictionary, and the
	  `INFOPLIST_KEY_*` build settings only express scalar top-level keys — setting
	  `INFOPLIST_KEY_NSExtensionPointIdentifier` did not produce this dictionary, and an
	  .appex without it cannot be installed at all: the simulator refuses the containing
	  app with "extensionDictionary must be set in placeholder attributes".

	  GENERATE_INFOPLIST_FILE stays YES for this target, so the display name and version
	  keys are still merged on top of this file.

	  A SwiftUI `WidgetBundle` marked `@main` is the entry point, so no
	  NSExtensionPrincipalClass is required — WidgetKit finds the bundle itself.
	-->
	<key>NSExtension</key>
	<dict>
		<key>NSExtensionPointIdentifier</key>
		<string>com.apple.widgetkit-extension</string>
	</dict>
</dict>
</plist>
