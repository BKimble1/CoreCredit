`Inter-SemiBold.ttf` renders the status-bar clock and the battery numerals,
matched to the reference capture's ink height, stroke weight and tabular digit
advance.

`Inter-800.ttf` (ExtraBold) and `Inter-500.ttf` (Medium) set Image 2's headline
and subtitle. Both weights were identified by matching Image 3's own rendered
copy: against its `Track`, Inter ExtraBold scores 0.88 IoU at best alignment,
where the nearest other face available here reaches 0.78.

Inter is used under the SIL Open Font License 1.1 — see `Inter-LICENSE.txt`.
